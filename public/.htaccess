RewriteEngine On
RewriteBase /

# Send all requests to index.html except files that actually exist
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
